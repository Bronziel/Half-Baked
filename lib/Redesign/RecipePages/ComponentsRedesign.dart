import 'package:flutter/material.dart';

class Standard40wallWidget extends StatelessWidget {
  const Standard40wallWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 40,
    );
  }
}
