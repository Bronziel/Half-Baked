import 'package:flutter/material.dart';

class minie3 extends StatelessWidget {
  const minie3({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
